<% if can? :manage, @user%>
<%@users.each do |user| %>
	<%= user.email%> 
	<%= link_to 'Delete', admin_destroy_user_path(user), method: :delete, confirm: "Are you sure?" %>
	<%= link_to 'Edit', edit_user_path(user)%>
	<br>

<%end%>
<%end%>